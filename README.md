## HOW TO TEST THIS APP

To test the application download the composer with the libraries that are in composer.json:

<code>php composer.phar install</code>

After installing the composer run the unit tests:

<code>vendor/bin/phpunit</code>

Use Git Bash for a good test.